#
# Generic utilities
#
include Files

OCAMLINCLUDES += ../stdlib

OCamlLibrary(lmutil, $(UTIL_FILES))

if $(defined LMINSTALL)
    OCamlLibraryCopy($(LMINSTALL), $(LIB), lmutil, $(UTIL_FILES))

clean:
    $(CLEAN)
