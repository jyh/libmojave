#
# Library
#
include Files

OCAMLINCLUDES += ../stdlib ../unix

if $(defined LMINSTALL)
    OCamlLibraryInstall($(LMINSTALL), $(LIB), lmsystem, $(SYSTEM_FILES))
else
    OCamlLibrary(lmsystem, $(SYSTEM_FILES))

clean:
    $(CLEAN)
