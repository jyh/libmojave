#
# Library
#
include Files

OCAMLINCLUDES += ../stdlib ../util

if $(defined LMINSTALL)
    OCamlLibraryInstall($(LMINSTALL), $(LIB), lmunix, $(UNIX_FILES))

clean:
    $(CLEAN)

all: lmunix$(LIB_SUFFIX)
